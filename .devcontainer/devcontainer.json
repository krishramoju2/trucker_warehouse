{
  "name": "Warehouse Management",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "runArgs": ["--network=host"],
  "postCreateCommand": "sudo service postgresql start && createdb -U postgres warehouse_db && psql -U postgres -d warehouse_db -f backend/db/schema.sql",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-azuretools.vscode-docker",
        "ms-python.python"
      ]
    }
  },
  "features": {}
}
